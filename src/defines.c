#include "defines.h"


const short int MAX_LINE = 1024;//Defining Max Limit for Each row in CSV file.
const short int MAX_TICKET_LIMIT = 6;//Defines the maximum limit of tickets that can be booked per ticket         

void kill_program(){
    exit(1);
}